context:
  name: python-kaleido
  version: "1.4.0"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/k/kaleido/kaleido-${{ version }}.tar.gz
    sha256: 7b893e3e24e28f180844fcf1b0255d1bc3787377507b4669a555a74bcf1d1f92

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . --no-deps -vv --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools >=65.0.0
    - setuptools-git-versioning
    - wheel
  run:
    - python >=${{ python_min }}
    - choreographer >=1.3.0
    - logistro >=1.0.8
    - orjson >=3.10.15
    - packaging
tests:
  - python:
      imports:
        - kaleido
      python_version: ${{ python_min }}.*

about:
  homepage: https://github.com/plotly/Kaleido
  license: MIT
  license_file: LICENSE.md
  summary: Fast static image export for web-based visualization libraries
  description: |
    Python interface to Kaleido, a cross-platform library for generating static images
    (e.g. png, svg, pdf, etc.) for web-based visualization libraries, with a
    particular focus on eliminating external dependencies.

extra:
  recipe-maintainers:
    - jonmmease
    - ytausch
